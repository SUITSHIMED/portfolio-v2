/**
 * AtmosphereNoise — full-screen grain overlay sitting above content,
 * just below the cursor. Pure CSS data-uri, pointer-events: none.
 */
function AtmosphereNoise() {
  return <div className="atmosphere-noise" aria-hidden="true" />;
}

export default AtmosphereNoise;
